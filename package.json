{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Simple json database to deploy to the host of your choice",
  "main": "server.js",
  "scripts": {
    "start" : "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json-server,heroku, node, REST API"
  ],
  "author": "Anvar Akhmadjanov",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
